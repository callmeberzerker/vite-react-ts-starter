{
  "name": "Hasura @ Cloud",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "Default GraphQL Endpoint": {
        "url": "https://direct-stag-12.hasura.app/v1/graphql",
        "headers": {
          "x-hasura-admin-secret": "${env:VITE_HASURA_ADMIN_PASSWORD}"
        },
        "introspect": false
      }
    }
  }
}